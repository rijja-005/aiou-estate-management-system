import { NextResponse } from 'next/server'; import { z } from 'zod'; import { requireRequestContext } from '../../../../server/api/auth-context'; import { errorResponse,successResponse } from '../../../../server/api/response'; import { validateCsrf } from '../../../../server/auth/csrf'; import { createAndRunReport,listReportJobs,REPORT_TYPES } from '../../../../server/reports/service';
const schema=z.object({reportType:z.enum(REPORT_TYPES),format:z.enum(['csv','json']).default('csv')});
export async function GET():Promise<NextResponse>{try{const actor=await requireRequestContext('report.read');return NextResponse.json(successResponse(await listReportJobs(actor.user.id,actor.user.permissions.includes('report.manage'))));}catch{return NextResponse.json(errorResponse('REPORT_LIST_FAILED','Unable to list report jobs'),{status:500});}}
export async function POST(request:Request):Promise<NextResponse>{if(!(await validateCsrf(request)))return NextResponse.json(errorResponse('CSRF_INVALID','Invalid CSRF token'),{status:403});try{const actor=await requireRequestContext('report.read');const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json(errorResponse('VALIDATION_ERROR','Invalid report request'),{status:400});return NextResponse.json(successResponse(await createAndRunReport(parsed.data.reportType,parsed.data.format,actor.user.id)),{status:201});}catch{return NextResponse.json(errorResponse('REPORT_CREATE_FAILED','Unable to generate report'),{status:500});}}
