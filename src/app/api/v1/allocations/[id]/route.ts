import { NextResponse } from 'next/server'; import { requireRequestContext } from '../../../../../server/api/auth-context'; import { errorResponse, successResponse } from '../../../../../server/api/response'; import { getAllocation } from '../../../../../server/allocations/service'; type Context = { params: Promise<{ id: string }> };
export async function GET(_request: Request, context: Context): Promise<NextResponse> { try { await requireRequestContext('allocation.read'); const row = await getAllocation((await context.params).id); return row ? NextResponse.json(successResponse(row)) : NextResponse.json(errorResponse('ALLOCATION_NOT_FOUND', 'Allocation not found'), { status: 404 }); } catch { return NextResponse.json(errorResponse('AUTH_UNAUTHORIZED', 'Authentication required'), { status: 401 }); } }
