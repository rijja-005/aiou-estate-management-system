import { requireRequestContext } from '../../../../../../server/api/auth-context'; import { getAllocation } from '../../../../../../server/allocations/service'; type Context = { params: Promise<{ id: string }> };
function escape(value: string): string { return value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;'); }
export async function GET(_request: Request, context: Context): Promise<Response> { await requireRequestContext('allocation.read'); const row = await getAllocation((await context.params).id); if (!row) return new Response('Not found', { status: 404 }); const title = row.status === 'VACATED' ? 'Office Vacation Record' : 'Office Allocation Letter'; const html = `<!doctype html><html><head><meta charset="utf-8"><title>${title}</title><style>body{font-family:Arial;max-width:800px;margin:48px auto;line-height:1.6}h1{color:#991b1b}.meta{border:1px solid #ddd;padding:20px} @media print{button{display:none}}</style></head><body><button onclick="print()">Print / Save PDF</button><h1>${title}</h1><div class="meta"><p><strong>Reference:</strong> ${escape(row.referenceNumber)}</p><p><strong>Office:</strong> ${escape(row.property.propertyCode)} — ${escape(row.property.displayName)}</p><p><strong>Department:</strong> ${escape(row.department.name)}</p><p><strong>Responsible person:</strong> ${escape(row.responsiblePerson ?? 'Not specified')}</p><p><strong>Status:</strong> ${row.status}</p><p><strong>Start:</strong> ${row.startDate?.toLocaleDateString('en-PK') ?? 'Pending'}</p><p><strong>End:</strong> ${row.endDate?.toLocaleDateString('en-PK') ?? 'Open'}</p></div></body></html>`; return new Response(html, { headers: { 'content-type': 'text/html; charset=utf-8', 'content-disposition': `inline; filename="${row.referenceNumber}.html"` } }); }
