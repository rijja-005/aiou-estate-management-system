import { requireRequestContext } from '../../../../../../server/api/auth-context'; import { prisma } from '../../../../../../server/db/prisma';
function esc(value: string): string { return value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;'); }
export async function GET(_request: Request, context: RouteContext<'/api/v1/payments/[id]/receipt'>): Promise<Response> { await requireRequestContext('finance.read'); const row = await prisma.payment.findUnique({ where: { id: (await context.params).id }, include: { receipt: true, tenant: true, allocations: { include: { bill: true } } } }); if (!row?.receipt) return new Response('Receipt not found', { status: 404 }); const html = `<!doctype html><html><head><meta charset="utf-8"><title>${esc(row.receipt.receiptNumber)}</title><style>body{font-family:Arial;max-width:760px;margin:48px auto;line-height:1.6}h1{color:#991b1b}.box{border:1px solid #ccc;padding:24px}@media print{button{display:none}}</style></head><body><button onclick="print()">Print / Save PDF</button><h1>Payment Receipt</h1><div class="box"><p><b>Receipt:</b> ${esc(row.receipt.receiptNumber)}</p><p><b>Payment:</b> ${esc(row.paymentNumber)}</p><p><b>Tenant:</b> ${esc(row.tenant.name)}</p><p><b>Amount:</b> PKR ${row.amount.toFixed(2)}</p><p><b>Method:</b> ${esc(row.paymentMethod)}</p><p><b>Date:</b> ${row.receivedAt.toLocaleDateString('en-PK')}</p><p><b>Applied bills:</b> ${row.allocations.map((a) => `${esc(a.bill.billNumber)} (PKR ${a.amount.toFixed(2)})`).join(', ') || 'Advance credit'}</p><p><b>Status:</b> ${row.status}</p></div></body></html>`; return new Response(html, { headers: { 'content-type': 'text/html; charset=utf-8', 'content-disposition': `inline; filename="${row.receipt.receiptNumber}.html"` } }); }
