import { requireRequestContext } from '../../../../../../server/api/auth-context'; import { prisma } from '../../../../../../server/db/prisma'; function esc(v:string){return v.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}
export async function GET(_request:Request,context:RouteContext<'/api/v1/bills/[id]/notice'>):Promise<Response>{await requireRequestContext('finance.read');const row=await prisma.bill.findUnique({where:{id:(await context.params).id},include:{agreement:{include:{tenant:true,property:true}}}});if(!row)return new Response('Not found',{status:404});const html=`<!doctype html><html><head><meta charset="utf-8"><title>Payment Notice</title><style>body{font-family:Arial;max-width:760px;margin:48px auto;line-height:1.6}h1{color:#991b1b}@media print{button{display:none}}</style></head><body><button onclick="print()">Print / Save PDF</button><h1>Rent and Arrears Notice</h1><p><b>Bill:</b> ${esc(row.billNumber)}</p><p>To: ${esc(row.agreement.tenant.name)}</p><p>Property: ${esc(row.agreement.property.displayName)}</p><p>Outstanding balance: <b>PKR ${row.balanceAmount.toFixed(2)}</b></p><p>Due date: ${row.dueDate.toLocaleDateString('en-PK')}</p><p>Please arrange payment through the Estate Office and retain the issued receipt.</p></body></html>`;return new Response(html,{headers:{'content-type':'text/html; charset=utf-8','content-disposition':`inline; filename="notice-${row.billNumber}.html"`}})}
