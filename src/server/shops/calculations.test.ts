import { describe, expect, it } from 'vitest'; import { calculateLateFee, nextBillStatus, proratedRent } from './calculations';
describe('shop billing calculations', () => { it('prorates rent using decimal arithmetic', () => expect(proratedRent('31000', 10, 31).toFixed(2)).toBe('10000.00')); it('calculates late fees without floating-point money', () => expect(calculateLateFee('12500', '2.5').toFixed(2)).toBe('312.50')); it('derives partial and paid states', () => { expect(nextBillStatus('1000', '0')).toBe('ISSUED'); expect(nextBillStatus('1000', '400')).toBe('PARTIALLY_PAID'); expect(nextBillStatus('1000', '1000')).toBe('PAID'); }); });
