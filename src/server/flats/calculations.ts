export function addMonthsClamped(date: Date, months: number): Date { if (!Number.isInteger(months) || months < 0) throw new Error('INVALID_MONTH_COUNT'); const year = date.getUTCFullYear(); const month = date.getUTCMonth(); const day = date.getUTCDate(); const targetFirst = new Date(Date.UTC(year, month + months, 1, date.getUTCHours(), date.getUTCMinutes(), date.getUTCSeconds(), date.getUTCMilliseconds())); const lastDay = new Date(Date.UTC(targetFirst.getUTCFullYear(), targetFirst.getUTCMonth() + 1, 0)).getUTCDate(); targetFirst.setUTCDate(Math.min(day, lastDay)); return targetFirst; }
export function retirementVacationDate(retirementDate: Date, postRetirementMonths = 6): Date { return addMonthsClamped(retirementDate, postRetirementMonths); }
